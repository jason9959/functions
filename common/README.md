# 공통 모듈

여러 기능이 함께 사용하는 데이터 조회, 상태 관리, 화면 공통 요소를 이곳에 둡니다.

분석 계산과 기능별 차트는 각 `features/featureXX_*` 폴더에 남깁니다.
현재 1~5번은 기존 화면을 보존하기 위해 호환 어댑터 단계로 연결되어 있으며,
다음 작업에서 각 기능의 계산·차트·보고서를 이 폴더 구조로 단계적으로 이동합니다.
